# Docker Notes

The initial local stack is defined in the repository root `docker-compose.yml`.

This directory is reserved for future custom images, admin UI containers, or job-runner Docker assets.
